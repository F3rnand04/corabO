const { onRequest } = require('firebase-functions/v2/https');
  const server = import('firebase-frameworks');
  exports.ssrcorabodc0a9 = onRequest({}, (req, res) => server.then(it => it.handle(req, res)));
  