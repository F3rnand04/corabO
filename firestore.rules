rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regla temporal para depuración: Permite el acceso público a toda la base de datos.
    // Esto es inseguro para producción y debe ser revertido.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
