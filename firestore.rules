
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are open for development and are not secure.
    // Restrict access before deploying to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
