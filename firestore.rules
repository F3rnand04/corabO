rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
  
    // Regla flexible para fase de prototipado:
    // Permite leer y escribir en CUALQUIER colección si el usuario está autenticado.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
