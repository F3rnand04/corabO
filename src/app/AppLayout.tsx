
'use client';

// **PRUEBA DE AISLAMIENTO: Contenido temporalmente neutralizado**
// Este componente ha sido simplificado al máximo para verificar si es la fuente
// del error 404. La lógica compleja ha sido eliminada.
// Si la app carga con este cambio, confirma que el error está en la interacción
// entre el Layout y los contextos, y procederemos a reconstruirlo correctamente.

export function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
